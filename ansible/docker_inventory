#!/bin/sh
# Backward-compatible shim: invokes the new
# `cmdbsyncer ansible inventory ansible` CLI inside the Syncer container.
# Replaces the old wrapper that called the now-removed `ansible source`.
exec docker exec -it cmdb_syncer-api-1 /srv/cmdbsyncer ansible inventory ansible "$@"

#!/usr/bin/env python3
"""CMDB Syncer CLI wrapper for source checkouts.

Pip-installed users get the same entry point as ``cmdbsyncer`` on PATH via
``[project.scripts]``. This thin wrapper keeps the repo-root workflow alive.
"""
from application.cli import main

if __name__ == "__main__":
    main()

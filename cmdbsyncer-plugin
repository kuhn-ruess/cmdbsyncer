#!/usr/bin/env python3
# pylint: disable=invalid-name
"""CMDB Syncer Plugin CLI wrapper for source checkouts.

Matches the pip-installed ``cmdbsyncer-plugin`` entry point
(``[project.scripts]``), so source- and pip-based installs share the
same command name.
"""
from application.plugins_cli import main

if __name__ == "__main__":
    main()

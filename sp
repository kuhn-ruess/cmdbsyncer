#!/usr/bin/env python3
"""CMDB Syncer Plugin CLI wrapper for source checkouts.

Pip-installed users get ``cmdbsyncer-plugin`` on PATH via ``[project.scripts]``.
"""
from application.plugins_cli import main

if __name__ == "__main__":
    main()

services:

  mongo:
    image: mongo:8.0
    volumes:
      - data_db:/data/db

  api:
    build:
      context: .
    depends_on:
      - mongo
    environment:
      config: base

volumes:
  data_db:
